server:
  port: 8050

spring:
  application:
    name: reservacionesHistorial
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reservasHistorygestion?useSSL=false&serverTimezone=UTCallowPublicKeyRetrieval=true
    username: mysqluser
    password: 141198
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
  config:
    import: optional:configserver:http://localhost:8888
  security:
    jwt:
      key:
        private: D130eU2ffROd+8QcHSLHzUURp4Bsy8DHSmrh8I/BDqQ
      user:
        generator: ANG_KEY
